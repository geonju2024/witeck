# 가상환경
venv/
__pycache__/
*.pyc

# 원본 영상 (약 1GB. GitHub 파일/저장소 용량 제한을 넘고, 인물이 식별되는 영상입니다)
videos/

# 랜드마크 중간 캐시. videos/ 에서 재생성 가능하므로 제외.
landmarks/
# 파일명 기반으로 저장하던 구버전 캐시. 영상 이름이 바뀌어 재사용 불가.
landmarks_old_flat/

# 데이터는 전부 Google Drive 공유 드라이브(WITECH)에 둡니다. 경로는 paths.py 참고.
#   WITECH/videos/           원본 영상
#   WITECH/derived/          labels.csv, landmarks/, dataset.npz, runs/
# 이 저장소에는 파이썬 코드만 커밋합니다.

# 아래 둘은 WITECH/archive/2026-08-08_local/ 로 옮겼습니다.
# 다시 생기더라도 커밋되지 않도록 남겨 둡니다.
old_dataset/
runs/
.venv/
__pycache__/
*.pyc
